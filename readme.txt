Data Format for processing to json:
CSV file with fields:
1 - Series/Collection
2 - Book Title
(3 - Chapter Title)
4 - Length (if chapter title exists, length will be treated as chapter length and book length calculated form that, otherwise length treated as book length)